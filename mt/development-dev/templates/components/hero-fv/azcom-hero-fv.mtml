<mt:Ignore>
  Hero Slider First View Container — DYNAMIC SLIDE SUPPORT
  
  Animation timing is fully JS-driven (hero-fv.js):
    - Each slide displays for 5 seconds
    - Total loop = 5s × number of slides (auto-calculated)
    - Text appears 0.8s after slide start, staggered by 0.5s per line
  
  TO ADD A NEW SLIDE:
  1. Copy a slide block below (SetVar + Include azcom-hero-fv-item)
  2. Update img_pc/img_sp to new image filenames
  3. Update line1/line2/line3 text (leave empty "" if not needed)
  4. Add a corresponding dot in Controls section (copy a button line)
  5. No CSS changes needed — JS calculates delays automatically
  
  Structure:
  - .c-hero-fv-wrap: wraps the slider section only
  - Controls are inside .c-hero-fv (position relative to the section)
  - azcom-hero-decoration is included OUTSIDE this wrap (in index.mtml)
</mt:Ignore>

<div id="js-hero-fv" class="c-hero-fv-wrap">
<section class="c-hero-fv" aria-label="メインビジュアル">
  <div class="c-hero-fv__container">

    <!-- Slide 1 -->
    <mt:SetVar name="img_pc" value="fv-img-001.png">
    <mt:SetVar name="img_sp" value="fv-sp-img-001.png">
    <mt:SetVar name="line1" value="物流の未来を、ともに創る">
    <mt:SetVar name="line2" value="">
    <mt:SetVar name="line3" value="">
    <mt:SetVar name="loading" value="eager">
    <mt:Include module="azcom-hero-fv-item">

    <!-- Slide 2 -->
    <mt:SetVar name="img_pc" value="fv-img-002.png">
    <mt:SetVar name="img_sp" value="fv-sp-img-002.png">
    <mt:SetVar name="line1" value="物流企業の未来を">
    <mt:SetVar name="line2" value="加速させるパートナー">
    <mt:SetVar name="line3" value="">
    <mt:SetVar name="loading" value="lazy">
    <mt:Include module="azcom-hero-fv-item">

    <!-- Slide 3 -->
    <mt:SetVar name="img_pc" value="fv-img-003.png">
    <mt:SetVar name="img_sp" value="fv-sp-img-003.png">
    <mt:SetVar name="line1" value="物流業界の課題を解決し">
    <mt:SetVar name="line2" value="パートナー企業の">
    <mt:SetVar name="line3" value="成長エンジンに">
    <mt:SetVar name="loading" value="lazy">
    <mt:Include module="azcom-hero-fv-item">

  </div>

  <!-- Controls — position:absolute inside .c-hero-fv -->
  <!-- TO ADD DOT: copy a button line, increment data-slide and aria-label numbers -->
  <div class="c-hero-fv__controls" aria-label="スライドコントロール">
    <button class="c-hero-fv__dot is-active" data-slide="1" aria-label="スライド1"></button>
    <button class="c-hero-fv__dot" data-slide="2" aria-label="スライド2"></button>
    <button class="c-hero-fv__dot" data-slide="3" aria-label="スライド3"></button>
    <!-- Add more dots here if you add more slides -->
    <button type="button" class="c-hero-fv__next" aria-label="次のスライドへ">
      <svg class="c-hero-fv__next-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M9 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
  </div>

</section>
</div>
